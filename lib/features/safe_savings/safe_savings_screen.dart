//C:\Users\user\asset_secretary\lib\features\safe_savings\safe_savings_screen.dart
import 'package:flutter/material.dart';

class SafeSavingsScreen extends StatelessWidget {
  const SafeSavingsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('안전자산')),
      body: const Center(child: Text('안전자산 화면(추가 예정)')),
    );
  }
}
